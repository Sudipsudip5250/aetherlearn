M6 browser verification, 2026-08-25: Local HTTP server at http://127.0.0.1:4173/ loaded the static web shell successfully. The shared manifest loaded five lessons from copied Markdown assets, the page showed the privacy note and Android-only Termux note, and five Read lesson buttons were visible. The first click attempt was issued while the page was scrolled to the lesson cards; a follow-up browser view is needed to confirm hash navigation and the reader state.

Second M6 browser checkpoint, 2026-08-25: The page still displayed the five lesson cards and their Read lesson controls after the first click attempt. A console-triggered click was issued; the console wrapper did not expose a return value, so route state should be checked with a direct location/hash assertion or a browser navigation URL before considering reader navigation verified.

Reader verification, 2026-08-25: Direct route `#/lesson/dl-01-digital-information` loaded the full shared lesson. DOM assertion confirmed `heroHidden=true`, `listHidden=true`, `readerHidden=false`, exactly one `.objectives-section`, and the expected reader title. The screenshot still showed the page top while the reader began below, but the DOM route state was correct.

Offline-layer browser debugging, 2026-08-25: After introducing the service worker, the browser initially served an older cached `app.js` and showed `markdown is not defined` despite the source fix. The service-worker cache was bumped to v2, then the test registration and caches were explicitly cleared through the browser console. A fresh load was required to verify the current script.

M6 offline-cache checkpoint, 2026-08-25: After clearing the stale service-worker cache, the current app loaded all five lessons. Clicking `Cache core content` succeeded; the UI reported `Cached 5 core lessons in this browser.` and the catalog changed to `cached core pack` with a green status indicator.

Preliminary local-state checks, 2026-08-25: The first offline-looking reload and reader/state checks were performed before the surviving child process of the local HTTP server was discovered. They established the intended UI behavior but are not treated as proof of network-disabled operation. The corrected genuine offline evidence follows below.

M6 quiz checkpoint, 2026-08-25: Filling `Four`, `Yes`, and `Text` then pressing `Check answers` produced `100% — 3 of 3 correct`; the feedback said the best result is stored locally. Reader controls for bookmark, private note, and completion were present.

M6 local-state checkpoint, 2026-08-25: Pressing Bookmark lesson changed the control to `Bookmarked`; the reader showed `1 attempt · best 100%` and retained the note. Pressing Mark lesson complete changed the status pill to `completed` and the button to `Completed`.

M6 progress/search checkpoint, 2026-08-25: Progress showed `1/5 completed`, `1 bookmarked`, and `1 with notes`, with the first lesson listed as `Completed · bookmarked · note saved`. Search accepted `algorithm` and returned two matches from the five-lesson pack, including `Problems, algorithms, and precise instructions`.

M6 practice/Termux checkpoint, 2026-08-25: Practice rendered all five lesson exercises and five `Open lesson` controls. Navigation to `py-02-python-setup-expressions-values` rendered the `Termux is Android-only.` notice while retaining lesson, practice, and knowledge-check content.

M6 persistence checkpoint, 2026-08-25: After a reload, the first lesson still showed `completed`, `Bookmarked`, the saved private note, and `1 attempt · best 100%`. The browser console had no output after the reload, so the stale `markdown is not defined` runtime failure did not recur.

M6 storage inspection, 2026-08-25: Direct browser inspection reported `controller: true`, one service-worker registration, Cache Storage key `aetherlearn-shell-v2`, and an IndexedDB `core-active` record with exactly five entries: `dl-01-digital-information`, `dl-05-privacy-passwords-phishing`, `py-01-problems-algorithms-instructions`, `py-02-python-setup-expressions-values`, and `dev-01-terminal-command-line`.

M6 failure-recovery setup, 2026-08-25: Cache Storage inspection found the v2 shell plus dynamically cached manifest and five Markdown responses. The cached manifest was removed deliberately while the IndexedDB `core-active` pack was left untouched. A refresh attempt was not accepted as failure evidence because the local server child was still running at that point.

M6 genuine network-disabled checkpoint, 2026-08-25: The surviving local HTTP server child was stopped explicitly; `curl` confirmed no HTTP response. With the server actually stopped, reloading `#/learn` still rendered the cached shell, five lessons, and `Cached 5 core lessons in this browser.` Direct navigation to `#/lesson/dl-01-digital-information` then rendered the full reader with the retained `completed`, `Bookmarked`, saved note, and `1 attempt · best 100%` state. This is the authoritative desktop Chromium offline result; earlier preliminary checks are retained only for debugging history.
M6 authoritative offline Practice/Search checkpoint, 2026-08-25: With the local server still stopped, Practice rendered all five offline exercises and Search retained the `algorithm` query with two lesson matches. These are the authoritative offline route results, replacing the preliminary route checks made before the surviving server process was found.
M6 cache-refresh diagnostic, 2026-08-25: The first simulated failure did not produce a failure status because a previously in-flight service-worker dynamic cache write repopulated the removed manifest. A direct fetch then returned the valid cached JSON and showed the manifest present again. This was test setup interference, not evidence that a failed update replaced the active IndexedDB pack; the recovery path still needs a deterministic retry after quiescing the cache.
M6 cache-recovery checkpoint, 2026-08-25: After quiescing the service worker and confirming the manifest remained absent, pressing Cache core content produced `Cache update failed; previous cache kept. Unexpected token '<', "<!doctype "... is not valid JSON`. The catalog still showed `5 modules · cached core pack` and all five lesson cards, confirming the previous IndexedDB pack remained active after the failed refresh.
